{{#if user}}
<div class="row">
<div class="col-lg-12">
    <div class="panel panel-default">
        <div style="height: 50px;" class="panel-heading">
            <a href="/api/clients/new" class="btn btn-danger pull-right" role="button">New client</a>
        </div>
        <!-- /.panel-heading -->
        <div class="panel-body">
            <div class="dataTable_wrapper">
                <table class="table table-striped table-bordered table-hover" id="clients">
                    <thead>
                        <tr>
                            <th style="text-align: center;">#</th>
                            <th>Full Name</th>
                            <th>Frequency</th>
                            <th>Account</th>
                            <th>Address</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each clients}}
                        <tr>
                            <td style="text-align: center;margin: 0px;padding: 5px;width: 120px;">
                                <a href="/api/clients/edit/{{this._id}}" class="btn btn-primary btn-outline" role="button">
                                    <strong>Edit</strong>
                                </a>&nbsp;
                                <a href="/api/clients/remove/{{this._id}}" class="btn btn-danger btn-outline" role="button">
                                    <strong>Del</strong>
                                </a>
                            </td>
                            <td style="font-size:18px;">{{this.name}}</td>
                            <td style="font-size:18px;">{{this.frequency}}</td>
                            <td style="font-size:18px;">{{this.account}}</td>
                            <td style="font-size:18px;">{{this.address}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>
<!-- /.col-lg-12 -->
</div>

<!-- Page-Level Demo Scripts - Tables - Use for reference -->
<script src="http://code.jquery.com/jquery-3.3.1.min.js" 
integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" 
crossorigin="anonymous"></script>

<script>
    $(document).ready(function() {
        $('#clients').DataTable({
            responsive: true
        });
    });
</script>
{{/if}}